# Installing this will require GCC if it is installed then the reload via Rsync can be harmonised.

mkdir -p install_fswatch
cd install_fswatch

curl -L -O https://github.com/emcrisostomo/fswatch/releases/download/1.17.1/fswatch-1.17.1.tar.gz --output fswatch-1.17.1.tar.gz

tar -xvzf fswatch-1.17.1.tar.gz

